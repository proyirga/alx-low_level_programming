#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: any number
 * Return: return absolute value of an int
 */

int _abs(int n)
{
	if (n <= 0)
		return (-1 * n);
	else
		return (1);
}
