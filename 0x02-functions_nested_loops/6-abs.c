#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: any number
 * Return: return absolute value of an int
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-1 * n);
}
